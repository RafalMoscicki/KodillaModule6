package com.kodilla.good.patterns.challenges;

public class OrderRepositoryImpl implements OrderRepository {
    @Override
    public boolean createOrder(User user, String item, int quantity) {

        return true;
    }
}
